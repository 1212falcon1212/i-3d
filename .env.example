# Kopyala:  cp .env.example .env   ve değerleri doldur.
# Bu dosya compose tarafından okunur; .env commit EDİLMEZ.

# --- Veritabanı ---
MYSQL_ROOT_PASSWORD=degistir-beni
DB_NAME=i3d

# --- Uygulama sırları ---
# openssl rand -hex 32
JWT_SECRET=degistir-beni-64-karakterlik-rastgele-deger
# openssl rand -hex 32
MEILI_MASTER_KEY=degistir-beni-meili-master-key

# --- Host portları (çakışma olursa buradan değiştir) ---
FRONTEND_PORT=3200
BACKEND_PORT=8180
MYSQL_PORT=3316
REDIS_PORT=6390
MEILI_PORT=7710
MAILPIT_UI_PORT=8125
